export enum Alignment {
  isCentered = 'is-centered',
  isRight = 'is-right',
}

export enum Separator {
  hasArrow = 'has-arrow-separator',
  hasBullet = 'has-bullet-separator',
  hasDot = 'has-dot-separator',
  hasSucceeds = 'has-succeeds-separator',
}

export enum Size {
  isSmall = 'is-small',
  isMedium = 'is-medium',
  isLarge = 'is-large',
}
