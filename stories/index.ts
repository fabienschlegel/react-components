import {
  decorator,
  flexDecorator,
  fullDecorator,
  fullScreenDecorator,
  routingDecorator,
} from './utils';

export { decorator, flexDecorator, fullDecorator, fullScreenDecorator, routingDecorator };
